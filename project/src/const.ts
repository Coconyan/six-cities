export enum AppRoute {
  SignIn = '/login',
  Favorites = '/favorites',
  Room = '/room',
  Root = '/'
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
